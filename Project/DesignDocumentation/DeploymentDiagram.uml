@startuml
node "Web Server" as WS {
  artifact "Frontend Application" as FE
}

node "Application Server" as AS {
  artifact "Backend Service" as BS
}

node "Database Server" as DB {
  database "PostgreSQL" as PG
}

WS -- AS : HTTP/S
AS -- DB : JDBC
@enduml
